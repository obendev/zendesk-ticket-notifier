# .env.example - Environment variables for Zendesk Ticket Notifier

# Polling interval for new tickets (ms). Default: 15000
# POLLING_INTERVAL_MS=15000

# User-facing labels of custom statuses to watch (comma-separated). Default: (empty)
# TARGET_STATUS_LABELS="Open,Pending"

# Tags to include in the search query (comma-separated). Default: (empty)
# TARGET_TAGS="support,urgent"

# Target group name (best-match search). Default: (empty)
# TARGET_GROUP="MySupportGroup"

# Base search query for static filters. Default: (empty)
# BASE_SEARCH_QUERY="type:ticket string"

# API request timeout (ms). Default: 15000
# API_REQUEST_TIMEOUT_MS=15000

# Base path for ticket URLs. Default: /agent/tickets/
# ZENDESK_TICKET_URL_BASE="/agent/tickets/"
